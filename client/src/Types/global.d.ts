import { ReactNode } from "react";

export type children = {
  children: ReactNode;
};
export type CollectionCart = {
  image?: string;
  title?: string;
  btnVariant?: string;
};

export type headingTypes = {
  title?: string;
  desc?: string;
};
